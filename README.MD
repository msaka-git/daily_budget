Amelioration du script est possible.

La premiere version contient des fichiers pour enregistrer et retirer les donnees alors que
celui qui se trouve dans le repertoire utilise une base de donnee SQL.